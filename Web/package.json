{
	"private": true,
	"scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
		"prepare": "lerna run prepare",
		"build": "lerna run build",
		"link:local": "lerna link --force-local",
		"setup": "npm-run-all bootstrap link:local build"
	},
	"workspaces": [
	  "packages/*"
	],
	"devDependencies": {
		"lerna": "^2.11.0"
	}
}
